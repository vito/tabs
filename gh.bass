(provide [cli]
  (use (.durations)
       (*dir*/wget))

  (def gh-deb
    (wget:wget
      "https://github.com/cli/cli/releases/download/v2.4.0/gh_2.4.0_linux_amd64.deb"
      ./gh.deb))

  (def cli
    (from (linux/ubuntu)
      ($ apt update)
      ($ apt -y install $gh-deb))))
