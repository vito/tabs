(provide [wget]
  (use (*dir*/nix.bass))

  (def nixery
    (nix:nixery *dir*/flake.lock))

  (def image
    (nixery/wget))

  (defn wget [url filename]
    (subpath
      (from image
        ($ wget $url -O $filename))
      filename)))

(defn main [url]
  (emit (wget url ./index.html) *stdout*))
